import React from "react";

const ChatResident = () => {
    return (
        <div className="bg-accent min-h-screen flex items-center justify-center">
            <h1 className="text-secondary font-bold text-2xl">Chat as Resident Page</h1>
        </div>
    );
};

export default ChatResident;